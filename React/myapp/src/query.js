import data from "./companydata.js";
// console.log(data);
// Object.keys(data).map((program) => {
//   console.log(program);
// });
// Object.values(data).map((program) => {
//   console.log(program);
// });
const Subject = Object.values(data).map((program) => {
  Object.keys(program).map((pdata) => {
    
  });
});
console.log(Subject);
